<form method="post" action="{{ path('app_campervan_delete', {'id': campervan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ campervan.id) }}">
    <button class="btn">Delete</button>
</form>
